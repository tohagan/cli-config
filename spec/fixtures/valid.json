{
    // Schema version to support upgrades
    "_schema": "2.0",

    // Can have comments
    "port": 3000,

    "hostname": "pinkzinc.com,

    "enabled": false,

    // Files that trigger auto reload.
    "nestedstuff": {
        "xxx": [
            "yyy",
            "zzz"
        ],
        "more": [
            "kajwhf"
        ]
    },

    "logging": 2  // 1=DEBUG, 2=INFO, 3=WARN, 4=ERROR, 5=CRITICAL
}
