/*!
 * Module dependencies.
 */

var cliConfig = require('../lib/index');

/*!
 * Specification: cli-config
 */

describe('cli-config - text exported interface methods', function() {
    it('should be an object', function() {
        expect(cliConfig).toEqual(jasmine.any(Object));
		
		it('cliConfig.getConfig should be a function', function() {
			expect(cliConfig.getConfig).toEqual(jasmine.any(Function));
		});
    });
});
